# Standard Library (no install needed):
# csv, math, random, pathlib, collections, dataclasses, typing, itertools

# Data Analysis (Recommended for analytics scripts)
pandas>=1.4.0
numpy>=1.22.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Enrichment (if used in future)
requests>=2.27.0
beautifulsoup4>=4.10.0
